# Make Us Rich - Serving

Serving is the process of serving a model to a client.

We use FastAPI to serve our model through a REST API.
The API is consumed by the interface which plot the differents predictions.